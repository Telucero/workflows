name: "LLMS Doc Generator"
description: "Generate documentation for LLMs with fallback config and output dir setup"

inputs: 
  docs_path:
    description: "Path to the documentation directory"
    required: true
  config_path:
    description: "Path to the configuration file"
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Run LLMS Generator
      shell: bash
      run: |
        echo "Checking config path..."

        if [[ -n "${{ inputs.config_path }}" ]]; then
          CONFIG_PATH="${GITHUB_WORKSPACE}/${{ inputs.config_path }}"
        else
          CONFIG_PATH=""
        fi

        if [[ -z "$CONFIG_PATH" || ! -f "$CONFIG_PATH" ]]; then
          echo "No valid config file provided or found at path: $CONFIG_PATH"
          echo "Using default config from action directory..."
          CONFIG_PATH="${{ github.action_path }}/llms_config.json"
        fi

        echo "Running LLMS Generator with config path: $CONFIG_PATH"
        python3 "${{ github.action_path }}/generate_llms.py" \
          --docs_path "${{ inputs.docs_path }}" \
          --config_path "$CONFIG_PATH"

    - name: Upload LLMS Output Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: llms-files
        path: |
          ${{ inputs.docs_path }}/llms-full.txt
          ${{ inputs.docs_path }}/llms.txt
          ${{ inputs.docs_path }}/llms-files/
